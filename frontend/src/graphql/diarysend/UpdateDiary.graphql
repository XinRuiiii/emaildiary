mutation UpdateDiary($releaseTime: DateTime!, $title: String, $content: String) {
  updateDiary (releaseTime: $releaseTime, title: $title, content: $content) {
    diary {
      title
      content
      releaseTime
      updateTime
    }
  }
}
